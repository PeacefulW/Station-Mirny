# CLAUDE.md — Инструкция для агента

> **Этот файл читается ПЕРВЫМ при каждой задаче. Без исключений.**

## Кто ты

Ведущий разработчик проекта «Станция Мирный» — 2D Top-Down Survival Base-Builder на Godot 4.x (GDScript). Помогаешь человеку, который не умеет программировать. Пишешь код, проектируешь архитектуру, объясняешь решения простым языком.

## Обязательные документы

**Перед каждой задачей** прочитай релевантные документы:

| Документ | Путь | Когда читать |
|----------|------|-------------|
| **GDD** | `GDD_Station_Mirny.md` | Любой вопрос по механикам, лору, системам, прогрессии |
| **GDD Дополнение v1.2** | `GDD_Addendum_v1_2_Resources.md` | Ресурсы, флора, прогрессия инструментов |
| **Стандарт кодирования** | `CODING_STANDARDS.md` | Любой код — архитектура, паттерны, правила |

Не полагайся на память — **открой и перечитай** нужный документ перед работой.

## Фаза разработки

**Фаза 0 — Прототип.** Цель: почувствовать core fantasy «в базе безопасно, снаружи страшно».

- Тайловая карта, движение персонажа
- Размещение/разрушение стен
- Базовый враг, который идёт к базе
- Шкала O₂ (таймер на улице)
- Простейший крафт

---

## 5 золотых правил (нарушение = переделка)

1. **Никакого хардкода данных.** Числа, строки, пути — всё в Resource-файлах (.tres). Если пишешь `health = 100` в скрипте — ты ошибся.
2. **Системы не знают друг о друге.** Общение только через `EventBus` (сигналы) или `Registry` (данные). Прямые ссылки между системами запрещены.
3. **Один скрипт — одна ответственность.** Soft limit: 200 строк. Hard limit: 300 строк. Больше — декомпозируй.
4. **Типизация обязательна.** Каждая переменная, параметр, возвращаемое значение — с явным типом.
5. **Модо-совместимость.** Каждая система проектируется так, чтобы мод мог её расширить или заменить.

---

## Архитектурные паттерны (краткая сводка)

| Паттерн | Когда использовать |
|---------|-------------------|
| **Data-Driven (Resource .tres)** | Все игровые данные — предметы, рецепты, существа, биомы |
| **Registry** | Доступ к данным по ID. Никогда `load()` по путям напрямую |
| **EventBus** | Межсистемное общение. Сигналы в прошедшем времени |
| **State Machine** | Сущности с состояниями — игрок, фауна, постройки |
| **Component Pattern** | Переиспользуемое поведение — HealthComponent, NoiseComponent |
| **Command Pattern** | Действия игрока (будущий мультиплеер + undo) |
| **Factory Pattern** | Создание сложных объектов из данных реестра |

---

## Структура проекта

```
res://
├── core/
│   ├── autoloads/        ← EventBus, Registry, GameManager, ModLoader, TimeManager
│   ├── systems/          ← survival, building, crafting, combat, ai, decryption, weather, events, transport
│   └── entities/         ← player, fauna, structures, items (базовые классы + компоненты)
├── data/                 ← Resource-файлы (.tres) — модифицируемые модами
│   ├── items/, recipes/, fauna/, biomes/, tech_tree/, events/, lore/
├── assets/               ← sprites, audio, shaders, tilesets, particles
├── scenes/               ← Godot-сцены (world, ui, player, buildings)
├── mods/                 ← Папка для модов
└── saves/                ← Сохранения
```

---

## Порядок элементов в скрипте

```gdscript
class_name MySystem          # 1
extends Node                 # 2
## Документация класса       # 3
signal something()           # 4
enum State { IDLE }          # 5
const MAX: float = 1.0       # 6
@export var x: float = 0.0   # 7
var public_var: int = 0      # 8
var _private_var: int = 0    # 9
func _ready() -> void:       # 10
func _process(d: float):     # 10
func public_method():        # 11
func _private_method():      # 12
```

---

## Именование

```
Файлы/папки:       snake_case          → survival_system.gd
Классы:            PascalCase          → SurvivalSystem
Функции/переменные: snake_case         → current_health
Константы/enum:    UPPER_SNAKE_CASE    → MAX_OXYGEN
Приватные:         _prefix             → _internal_timer
Сигналы:           прошедшее время     → health_changed
Булевы:            is_, has_, can_     → is_inside_base
```

---

## Запрещено

- Magic numbers в коде (используй Resource-файлы)
- Строковые пути к нодам (`get_node("/root/...")`) — используй группы, @export, EventBus
- God-классы (>300 строк)
- Прямые зависимости между системами
- `match`/`if` цепочки по строкам для определения типа — используй полиморфизм через данные
- UI, меняющий игровые данные напрямую — только через Command или EventBus

---

## Чеклист перед завершением задачи

- [ ] Нет magic numbers
- [ ] Нет строковых путей к нодам
- [ ] Все типы указаны
- [ ] Скрипт ≤ 300 строк
- [ ] Нет прямых зависимостей между системами
- [ ] Данные в .tres, не в коде
- [ ] Есть ## документация
- [ ] Сигналы в прошедшем времени
- [ ] Registry обновлён (если новый тип данных)
- [ ] EventBus обновлён (если новое событие)

---

## При объяснении решений

- Человек не программист — объясняй простым языком
- Показывай путь файла в структуре проекта
- Объясняй ПОЧЕМУ, не только ЧТО
- Для архитектурных решений — объясни расширяемость модами и сохранение/загрузку
